<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xmu.vis.mapper.NodeTypeTableMapper">

    <insert id="addNewNodeType" parameterType="xmu.vis.domain.NodeTypeTable">
        insert into NodeTypeTable (nodetypeid, nodetypename, nodetypeattribute) values (#{nodetypeid}, #{nodetypename}, #{nodetypeattribute})
    </insert>

    <select id="getNodeAttributeDictByNodeTypeId" parameterType="String" resultType="String">
        select nodetypeattribute from NodeTypeTable where nodetypeid=#{nodeTypeId}
    </select>
</mapper>